{
  "name": "route-x-bot",
  "version": "1.0.0",
  "module": "src/index.ts",
  "scripts": {
    "dev": "bun scripts/gen-conversations-version.mjs && bun run --watch src/index.ts",
    "start": "bun scripts/gen-conversations-version.mjs && bun run src/index.ts",
    "gen:version": "node scripts/gen-conversations-version.mjs",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate deploy",
    "db:push": "prisma db push",
    "vercel-build": "node scripts/gen-conversations-version.mjs && prisma generate && prisma migrate deploy"
  },
  "dependencies": {
    "@grammyjs/conversations": "^2.0.1",
    "@prisma/client": "^6.0.0",
    "grammy": "^1.31.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@vercel/node": "^5.9.9",
    "bun-types": "latest",
    "prisma": "^6.0.0",
    "typescript": "^5.0.0"
  }
}
